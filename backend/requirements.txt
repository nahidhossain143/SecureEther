# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6      # [standard] adds websocket + faster event loop support
python-multipart==0.0.9        # NEW: required for FastAPI file/form uploads

# ── Data & ML ─────────────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
xgboost==2.1.1
imbalanced-learn==0.12.3
shap==0.45.1
joblib==1.4.2
scipy==1.13.1

# ── API & Validation ──────────────────────────────────────────────────────────
pydantic==2.8.2
httpx==0.27.0                  # NEW: needed for async test client (pytest + FastAPI)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.2                  # NEW: unit testing
pytest-asyncio==0.23.8         # NEW: async route testing

# ── Dev / Quality of Life ─────────────────────────────────────────────────────
python-dotenv==1.0.1           # NEW: load .env config (API keys, paths, etc.)